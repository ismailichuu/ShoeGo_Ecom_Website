<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>ShoeGo | Verify</title>
    <link rel="shortcut icon" href="images/favicon.png" type="image/x-icon">
    <link rel="stylesheet" href="css/style.css">
    <link rel="stylesheet" href="css/output.css">
</head>

<body class="min-h-screen bg-gradient-to-r from-gray-900 via-gray-800 to-black flex items-center justify-center">
    <div class="min-h-screen flex items-center justify-center px-4">
        <div class="bg-white p-8 rounded-2xl shadow-2xl max-w-sm w-full text-center border border-gray-200">
            <h2 class="text-2xl font-bold text-gray-800 mb-4">Forgot Password?</h2>
            <p class="text-md text-gray-600 mb-6">
                Please enter your email<br><span id="email" class="font-medium italic text-gray-900">
                    example: shoego@123gmail.com
                </span>
            </p>
            <form action="/forgot" method="post">
                <!-- Email -->
                <input type="email" placeholder="Email" name="email"
                    class="w-full px-4 py-2 rounded-lg bg-gray-100 border border-gray-300 focus:outline-none focus:ring-2 focus:ring-gray-700"
                    required>
                <% if(msg) { %>
                    <p class="text-red-600 text-center" id="error">
                        <%= msg %>
                    </p>
                    <script>
                        setTimeout(() => {
                            document.getElementById('error').style.display = 'none';
                        }, 8000);
                    </script>
                    <% } %>

                        <button
                            class="w-full mt-3 bg-black hover:bg-gray-800 text-white font-semibold py-2 rounded-lg transition shadow">Confirm
                        </button>
            </form>
            <a href="/login" class="text-sm text-gray-900 mt-4 inline-block hover:underline">
                ← Back to Login
            </a>
        </div>
    </div>

    <script src="js/script.js"></script>
</body>

</html>