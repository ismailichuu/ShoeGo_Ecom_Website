<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>ShoeGo | Admin</title>
    <link rel="shortcut icon" href="images/favicon.png" type="image/x-icon">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/output.css">
</head>

<body class="min-h-screen bg-gradient-to-r from-gray-900 via-gray-800 to-black flex items-center justify-center">
    <div class="min-h-screen flex items-center justify-center px-4">
        <div class="bg-white p-8 rounded-2xl shadow-2xl max-w-sm w-full text-center border border-gray-200">
            <h2 class="text-2xl font-bold text-gray-800 mb-4">Admin</h2>
            </p>
            <form action="/admin/login" method="post">
                <!-- Email -->
                <input type="email" placeholder="Email" name="email"
                    class="w-full px-4 py-2 rounded-lg mb-4 bg-gray-100 border border-gray-300 focus:outline-none focus:ring-2 focus:ring-gray-700"
                    required>
                <!--Password-->
                <input type="password" placeholder="Password" name="password"
                    class="w-full px-4 py-2 rounded-lg mb-4 bg-gray-100 border border-gray-300 focus:outline-none focus:ring-2 focus:ring-gray-700"
                    required>
                    <% if(msg) { %>
                        <p class="text-red-600 text-center" id="error"><%= msg %></p>
                        <script>
                          setTimeout(() => {
                            document.getElementById('error').style.display = 'none';
                          }, 8000);
                        </script>
                        <% } %>
                <button
                    class="w-full mt-3 bg-black hover:bg-gray-800 text-white font-semibold py-2 rounded-lg transition shadow">Login
                </button>
            </form>
        </div>
    </div>

    <script src="js/script.js"></script>
</body>

</html>