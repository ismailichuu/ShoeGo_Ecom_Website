<div class="bg-gray-100 font-sans leading-normal tracking-normal">

  <nav class="bg-white p-4 shadow-md">
    <div class="max-w-7xl mx-auto flex justify-between items-center">
      <h1 class="text-2xl font-bold text-gray-800">Admin Dashboard</h1>
      <div class="flex items-center space-x-4">
        <span class="text-gray-600">Welcome, Admin!</span>
        <a href="/admin/signout" class="text-blue-600 hover:text-blue-800">Logout</a>
      </div>
    </div>
  </nav>

  <div class="max-w-7xl mx-auto p-6">

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
      <div class="bg-white p-6 rounded-lg shadow-lg flex items-center justify-between">
        <div>
          <p class="text-gray-500 text-sm font-medium">Total Sales</p>
          <p class="text-3xl font-bold text-gray-900 mt-1">₹<%= summary.totalSales.toLocaleString() %></p>
        </div>
        <div class="bg-blue-100 text-blue-600 p-3 rounded-full">
          <i class="fas fa-dollar-sign text-xl"></i>
        </div>
      </div>
      <div class="bg-white p-6 rounded-lg shadow-lg flex items-center justify-between">
        <div>
          <p class="text-gray-500 text-sm font-medium">New Orders</p>
          <p class="text-3xl font-bold text-gray-900 mt-1" id="newOrders"><%= summary.newOrders %></p>
        </div>
        <div class="bg-green-100 text-green-600 p-3 rounded-full">
          <i class="fas fa-shopping-cart text-xl"></i>
        </div>
      </div>
      <div class="bg-white p-6 rounded-lg shadow-lg flex items-center justify-between">
        <div>
          <p class="text-gray-500 text-sm font-medium">New Customers</p>
          <p class="text-3xl font-bold text-gray-900 mt-1" id="newCustomers"><%= summary.newCustomers %></p>
        </div>
        <div class="bg-purple-100 text-purple-600 p-3 rounded-full">
          <i class="fas fa-users text-xl"></i>
        </div>
      </div>
      <div class="bg-white p-6 rounded-lg shadow-lg flex items-center justify-between">
        <div>
          <p class="text-gray-500 text-sm font-medium">Total Returned</p>
          <p class="text-3xl font-bold text-gray-900 mt-1" id="pendingIssues"><%= summary.totalReturned %></p>
        </div>
        <div class="bg-red-100 text-red-600 p-3 rounded-full">
          <i class="fas fa-exclamation-triangle text-xl"></i>
        </div>
      </div>
    </div>

    <div class="mb-6 flex justify-end items-center">
      <label class="font-semibold text-gray-700 mr-2">Filter by:</label>
      <select id="filterSelect" class="border border-gray-300 px-4 py-2 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500">
        <option value="monthly">Monthly</option>
        <option value="yearly">Yearly</option>
        <option value="weekly">Weekly</option>
        <option value="daily">Daily</option>
        <option value="custom">Custom</option>
      </select>
    </div>
   <div class="grid grid-cols-1 lg:grid-cols-3 gap-6 mb-8">
  <!-- Top Products Chart -->
  <div class="lg:col-span-2">
    <h2 class="text-xl font-semibold text-gray-800 mb-4">Top Products by Sales</h2>
    <div class="bg-white p-6 rounded-lg shadow-lg h-96">
      <canvas id="productsChart" class="w-full h-full"></canvas>
    </div>
  </div>

  <!-- Top Categories Chart -->
  <div>
    <h2 class="text-xl font-semibold text-gray-800 mb-4">Top Categories by Sales</h2>
    <div class="bg-white p-6 rounded-lg shadow-lg h-96">
      <canvas id="categoriesChart" class="w-full h-full"></canvas>
    </div>
  </div>
</div>


    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 mb-8">
      <div>
      <h2 class="text-xl font-semibold text-gray-800 mb-4">Top Brands by Sales</h2>
      <div class="bg-white p-6 rounded-lg shadow-lg h-96 relative">
        <canvas id="brandsChart" class="absolute inset-0 w-full h-full"></canvas>
      </div>
      </div>
    </div>

    <div class="bg-white p-6 rounded-lg shadow-lg mb-8">
      <h2 class="text-xl font-semibold text-gray-800 mb-4">Top Selling Products</h2>
      <div class="overflow-x-auto">
        <table class="min-w-full divide-y divide-gray-200">
          <thead class="bg-gray-50">
            <tr>
              <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Product Name</th>
              <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Category</th>
              <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Brand</th>
              <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Units Sold</th>
              <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Revenue</th>
            </tr>
          </thead>
          <tbody class="bg-white divide-y divide-gray-200" id="topProductsTableBody">

          </tbody>

        </table>
      </div>
    </div>
    <!-- Loader Overlay -->
    <div id="dashboardLoader" class="fixed inset-0 z-50 bg-white bg-opacity-75 flex items-center justify-center hidden">
      <div class="w-12 h-12 border-4 border-blue-500 border-t-transparent rounded-full animate-spin"></div>
    </div>

  </div>

</div>